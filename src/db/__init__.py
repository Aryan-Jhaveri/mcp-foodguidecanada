"""
Database Operations

Handles SQLite database connections, schema management, and query operations
for Canada's Food Guide recipe data:
- Persistent storage: User favorites 
- Temporary storage: Recipe data with sub-tables per recipe session
"""